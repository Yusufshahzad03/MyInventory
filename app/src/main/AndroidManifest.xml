<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yusuf.myinventory">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".Menu" />
        <activity android:name=".ResetPassword" />
        <activity android:name=".ChangePasswordDelete" />
        <activity android:name=".Product" />
        <activity android:name=".AddProductInfo" />
        <activity android:name=".AddSalesOrder" />
        <activity android:name=".CustomerDetails" />
        <activity android:name=".ToDoList"/>
        <activity android:name=".Email">
       </activity>
    </application>

</manifest>